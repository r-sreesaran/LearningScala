/**
 * Created by sree on 22/09/14.
 */
object UtopianTree {
  def main(args: Array[String]) {
    var mapOfCycles = io.Source.stdin.getLines().take(3).map(_.toInt);
    mapOfCycles.drop(0);
    
  }
}
